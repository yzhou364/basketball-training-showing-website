{
  "name": "joshua-tabb-basketball-training",
  "version": "1.0.0",
  "private": true,
  "description": "Joshua Tabb basketball training — promotional site",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./tailwind.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./tailwind.css --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
